org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
org.gradle.caching=true
org.gradle.parallel=true
# JDK 21 required — Gradle toolchain auto-detects or downloads.
# If auto-detection fails, set org.gradle.java.home in ~/.gradle/gradle.properties
# pointing to your local JDK 21 installation.
