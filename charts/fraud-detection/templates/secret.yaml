apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
  labels:
    app: {{ .Release.Name }}
type: Opaque
stringData:
  DB_USERNAME: "{{ .Values.database.username }}"
  DB_PASSWORD: "{{ .Values.database.password }}"
